app:
  name: momentum-signals
  environment: local

api:
  host: 0.0.0.0
  port: 8000
  api_key: "change-me"

storage:
  db_path: "./momentum_signals.db"

sources:
  github:
    enabled: false
    token: ""
    repos:
      - "openai/openai-python"
      - "tiangolo/fastapi"
      - "pallets/flask"

scoring:
  weights:
    commits_30d: 0.35
    star_velocity: 0.25
    issues_30d: 0.15
    contributors: 0.15
    release_recency: 0.10
